CONCILIADOR - HACER AHORA

YA ESTA VALIDADO:
- Rama correcta: contpaq-mi4-integration
- Commit correcto: 764796d
- Backend correcto: app.py ya trae _query_in_chunks
- Plantilla correcta: contpaq_conciliacion.html ya trae syncTopCombos y select id="titulo"

1) REINICIAR LA APP
cd ~/industrial-verduzco
docker compose restart app

2) VALIDAR QUE SUBIO BIEN
docker compose ps
docker compose logs --tail=60 app

3) PROBAR EL CONCILIADOR DESDE EL SERVIDOR
curl -I http://localhost:5000/contpaq/conciliacion

4) SI EL curl RESPONDE 200 O 302, ABRIR LA PAGINA EN EL NAVEGADOR Y HACER RECARGA DURA
Ctrl + F5

5) SI SIGUE MAL, PEGAR ESTAS SALIDAS
docker compose logs --tail=120 app
curl -s http://localhost:5000/contpaq/conciliacion | head -n 20
